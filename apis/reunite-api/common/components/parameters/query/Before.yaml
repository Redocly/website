name: before
in: query
required: false
description: |-
  Use the `page.previousPage` cursor as a value for the `before` parameter to get the previous page.
schema:
  type: string
example: bfg23aksjf23zb1==
