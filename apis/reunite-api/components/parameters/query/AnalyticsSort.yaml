name: sort
description: |-
  Sorts the data by the specified field. Prefix a field with `-` for descending
  order (for example, `-time`) or omit the prefix for ascending order (for example, `time`).

  Defaults to `-time` (most recent first).
in: query
required: false
schema:
  type: string
  default: -time
example: -time
