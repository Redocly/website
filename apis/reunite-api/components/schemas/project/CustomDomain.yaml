type: object
properties:
  customDomain:
    description: Custom domain without protocol.
    type: [string, 'null']
    example: docs.example.com
  keepDefaultDomain:
    description: Keep default domain.
    type: boolean
    example: true
required:
  - customDomain
  - keepDefaultDomain
