title: GitLabSelfManagedAppSecretCredential
type: object
writeOnly: true
properties:
  appSecret:
    type: string
  # TODO: check if we still need this field, 
  # because in GitLab self-managed we can use the same secret as for GitLab Cloud
  webhookSecret:
    type: string
required:
  - appSecret
