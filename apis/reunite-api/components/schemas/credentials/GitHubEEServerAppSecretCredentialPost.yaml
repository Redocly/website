# TODO: Consider refactoring credentials `POST` and `PATCH` schemas to use the same schema (follow openapi-style guide)
title: GitHubEEServerAppSecretCredentialPost
type: object
allOf:
  - $ref: ./GitHubEEServerAppSecretCredential.yaml
required:
  - clientSecret
  - privateKey
