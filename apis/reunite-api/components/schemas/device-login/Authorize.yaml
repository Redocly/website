type: object
properties:
  client_id:
    type: string
    description: Client ID that initializes device login.
    example: client123
  client_name:
    type: string
    description: Client name for well known device login clients.
    example: redocly-cli

oneOf:
  - required: [client_id]
  - required: [client_name]

