type: object
description: Paginated list of subscriptions for support operations.
properties:
  object:
    type: string
    enum: [list]
  page:
    $ref: ../Page.yaml
  data:
    type: array
    minItems: 0
    items:
      $ref: ./SupportSubscription.yaml
required:
  - object
  - page
  - data
