type: object
properties:
  status: 
    description: Status of single operation.
    type: string
    const: 'ok'
    example: 'ok'
    readOnly: true
  action:
    description: Performed action of single operation.
    example: create
    type: string
    enum:
      - create
      - update
      - delete
    readOnly: true
  resource:
    $ref: ./ApiMonitoringJob.yaml
required:
  - status
  - action
  - resource
    