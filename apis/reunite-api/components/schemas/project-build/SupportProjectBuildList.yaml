type: object
description: Paginated list of project builds for support operations.
properties:
  object:
    type: string
    enum: [list]
  page:
    $ref: ../Page.yaml
  data:
    type: array
    minItems: 0
    items:
      $ref: ./SupportProjectBuild.yaml
  items:
    type: array
    minItems: 0
    items:
      $ref: ./SupportProjectBuild.yaml
    deprecated: true
required:
  - object
  - page
  - items
  - data
