type: object
properties:
  comment:
    type: string
    maxLength: 5000
    description: Free-form text feedback.
  reasons:
    type: array
    maxItems: 10
    description: List of preset reasons for feedback.
    items:
      type: string
      maxLength: 5000
      description: Reason for feedback.
