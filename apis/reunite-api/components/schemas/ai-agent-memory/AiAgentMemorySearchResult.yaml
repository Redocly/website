type: object
description: Semantic search results over the AI writer agent memory.
properties:
  documents:
    description: Most relevant memory documents first.
    type: array
    minItems: 0
    items:
      $ref: ./AiAgentMemoryDocument.yaml
required:
  - documents
