type: object
properties:
  destinationQueue:
    description: Queue which messages should be routed to.
    type: string
    example: mailer.send-invite-email
    writeOnly: true
  counter:
    description: Number of redelivered messages.
    type: integer
    minimum: 0
    example: 1
    readOnly: true
required:
  - destinationQueue
