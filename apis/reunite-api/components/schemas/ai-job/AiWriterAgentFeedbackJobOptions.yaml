type: object
description: Feedback trigger options for the AI writer agent job.
required:
  - trigger
  - feedbackId
  - metadata
properties:
  trigger:
    type: string
    enum:
      - FEEDBACK
  feedbackId:
    type: string
    minLength: 1
  metadata:
    $ref: ./AiWriterAgentJobMetadata.yaml
