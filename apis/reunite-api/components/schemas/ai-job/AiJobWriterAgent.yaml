allOf:
  - $ref: ./AiJobBase.yaml
  - type: object
    required:
      - type
      - options
    properties:
      type:
        type: string
        enum:
          - WRITER_AGENT
        readOnly: true
        description: Type of the AI job.
      options:
        readOnly: true
        description: Writer agent job options.
        oneOf:
          - $ref: ./AiWriterAgentJobOptions.yaml
          - type: 'null'
