type: object
description: Scheduled trigger options for the AI writer agent job.
required:
  - trigger
  - metadata
properties:
  trigger:
    type: string
    enum:
      - SCHEDULED
  metadata:
    $ref: ./AiWriterAgentJobMetadata.yaml
