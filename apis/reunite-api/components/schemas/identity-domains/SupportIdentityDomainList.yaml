type: object
description: Paginated list of identity domains for support operations.
properties:
  object:
    type: string
    enum: [list]
  page:
    $ref: ../Page.yaml
  data:
    type: array
    minItems: 0
    items:
      $ref: ./SupportIdentityDomain.yaml
  items:
    type: array
    minItems: 0
    items:
      $ref: ./SupportIdentityDomain.yaml
    deprecated: true
required:
  - object
  - page
  - items
  - data
