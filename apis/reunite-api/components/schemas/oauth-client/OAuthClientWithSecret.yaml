allOf:
  - $ref: ./OAuthClient.yaml
  - type: object
    properties:
      clientSecret:
        description: Client secret. Returned only in the create OAuth2 client response.
        type: string
        readOnly: true
    required:
      - clientSecret
