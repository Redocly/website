type: object
title: ProjectService
properties:
  id:
    description: >
      Project service identifier. Either a project service name (`project-{slug}`)
      or a custom domain (`docs.example.com`).
    type: string
    example: project-acme
    readOnly: true
  object:
    description: Entity name.
    type: string
    enum: [projectService]
    readOnly: true
required:
  - id
  - object
