type: string
enum:
  - organization
  - project
  - feature
description: |-
  How the limit row is scoped. `organization` rows cover the whole org;
  `project` rows are pinned to a single project; `feature` rows cap usage
  of a single feature (optionally further narrowed by project).
example: organization
