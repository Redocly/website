type: object
properties:
  status:
    type: string
    enum: [UPDATED, UPDATE_FAILED, LIMIT_EXCEEDED, SKIPPED]
    readOnly: true
  totalPages:
    type: integer
    example: 1
    readOnly: true
required:
  - status
  - totalPages