type: object
description: Code-change range trigger options for the AI writer agent job.
required:
  - trigger
  - range
  - metadata
properties:
  trigger:
    type: string
    enum:
      - CODE_CHANGE
  range:
    $ref: ./AiWriterAgentRangeOptions.yaml
  metadata:
    $ref: ./AiWriterAgentJobMetadata.yaml
