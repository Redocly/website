type: object
required:
  - url
  - key
properties:
  url:
    type: string
    description: Presigned S3 PUT URL. Valid for 5 minutes.
  key:
    type: string
    description: >-
      S3 object key for the uploaded file. Pass this value in
      `fileUrls[]` when creating the project.
