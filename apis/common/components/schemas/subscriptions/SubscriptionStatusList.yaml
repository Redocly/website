type: object
properties:
  object:
    type: string
    enum: [list]
  page:
    $ref: '../../schemas/Page.yaml'
  data:
    type: array
    items:
      $ref: './SubscriptionStatus.yaml'
required:
  - object
  - page
  - data